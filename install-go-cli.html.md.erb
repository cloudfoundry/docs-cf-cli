---
title: Installing the cf CLI
owner: CLI
---

This topic describes how to install and uninstall the Cloud Foundry Command Line Interface (cf CLI). 


## <a id="overview"></a> Overview

The cf CLI is the official command line client for Cloud Foundry. 

Follow the instructions in this topic to install the cf CLI on your operating system. You can install the cf CLI with a package manager, an installer, or a compressed binary.

For more information about how to use the cf CLI, see [Getting Started with cf CLI](./getting-started.html).

To learn when updates are released and to download a new binary or installer, see the [CLI releases page](https://github.com/cloudfoundry/cli/releases) on GitHub.


## <a id="prerequisites"></a> Prerequisites

If you previously used the cf CLI Ruby gem, you must uninstall the gem before installing the cf CLI. 

To uninstall the gem, do the following:

1. Run  `gem uninstall cf`.

1. Verify that your Ruby environment manager uninstalled the gem by closing and reopening your terminal.


## <a id="pkg"></a> Install the cf CLI Using a Package Manager

This section describes how to install the cf CLI using a package manager. You can install the cf CLI using a package manager on Mac OS X and Linux operating systems.


### <a id='pkg-linux'></a> Linux Installation

There are two ways to install the cf CLI using a package manager, depending on your Linux distribution.

To install the cf CLI on Debian and Ubuntu-based Linux distributions, do the following:

1. Add the Cloud Foundry Foundation public key and package repository to your system:

	```
	wget -q -O - https://packages.cloudfoundry.org/debian/cli.cloudfoundry.org.key | sudo apt-key add -
	```

	```
	echo "deb https://packages.cloudfoundry.org/debian stable main" | sudo tee /etc/apt/sources.list.d/cloudfoundry-cli.list
	```

1. Update your local package index:

	```
	sudo apt-get update
	```

1. Install the cf CLI:

	```
	sudo apt-get install cf-cli
	```

To install the cf CLI on Enterprise Linux and Fedora RHEL6/CentOS6 and later distributions, do the following:

1. Configure the Cloud Foundry Foundation package repository:
	
	```
	sudo wget -O /etc/yum.repos.d/cloudfoundry-cli.repo https://packages.cloudfoundry.org/fedora/cloudfoundry-cli.repo
	```

1. Install the cf CLI. The following command also downloads and adds the public key to your system.

	```
	sudo yum install cf-cli
	```


### <a id='pkg-mac'></a> Mac OS X Installation

You can install the cf CLI on Mac OS X operating systems using the Homebrew package manager.

To install the cf CLI for Mac OS X using Homebrew, do the following:

1. Install Homebrew. For instructions, see [Homebrew](http://brew.sh/).

1. Install the Cloud Foundry Homebrew package:

	```
	brew tap cloudfoundry/tap
	```

1. Install the cf CLI:

	```
	brew install cf-cli
	```


## <a id="bin"></a> Install the cf CLI Using a Compressed Binary

This section describes how to install the cf CLI using a compressed binary. You can install the cf CLI using a compressed binary on Windows, Mac OS X, and Linux operating systems.

For more information about downloading and installing a compressed binary, see the [Installers and compressed binaries](https://github.com/cloudfoundry/cli#installers-and-compressed-binaries) section of the cf CLI repository on GitHub.


## <a id="verify"></a> Verify Installation

To verify the installation of the cf CLI, do the following:

1. Close and reopen the command prompt. Or, open a new tab in the command prompt.

1. Run the following command:

	```
	cf
	``` 
	If your installation was successful, the cf CLI help listing appears.


## <a id="uninstall"></a> Uninstall the cf CLI

This section describes how to uninstall the cf CLI. There are different methods for uninstalling the cf CLI depending on the installtion method.

### <a id="uninstall-pkg"></a> Package Manager

This section describes how to uninstall the cf CLI if you installed the cf CLI with a package manager.

To uninstall the cf CLI, follow the instructions specific to your package manager.

### <a id="uninstall-installer"></a> Installer

This section describes how to uninstall the cf CLI if you installed the cf CLI with an installer. Follow the instructions in this section that are specific to your operating system.

To uninstall the cf CLI on Mac OS X, do the following:

1. Delete the binary `/usr/local/bin/cf`.

1. Delete the directory `/usr/local/share/doc/cf-cli`.

To uninstall the cf CLI on Windows, do the following:

1. Navigate to the **Control Panel** and click **Programs and Features**.

1. Select `Cloud Foundry CLI VERSION` and click **Uninstall**.

### <a id="uninstall-binary"></a> Binary

This section describes how to uninstall the cf CLI if you installed the cf CLI with a binary.

To uninstall the cf CLI, do the following:

1. Navigate to the location where you copied the binary.

1. Delete the binary.
